<UserControl x:Class="TaskManagement.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaskManagement.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--<StackPanel Grid.Row="0" Grid.Column="1">
            <Button Background="#e4e4e4" Content="Tasks" Width="150" HorizontalAlignment="Right" x:Name="DisplayTasksArchieve"/>
        </StackPanel>-->

        <ContentControl Grid.Row="1" Grid.Column="0" x:Name="LeftContainer" Margin="0 10 0 0"/>
        <ContentControl Grid.Row="1" Grid.Column="1" x:Name="RightContainer" Margin="0 10 0 0"/>

    </Grid>
</UserControl>